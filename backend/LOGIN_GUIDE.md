# 🔑 登录账户快速参考

## 📋 所有可用账户

### 管理员账户 ✅ 已修复
```
用户名: admin
邮箱: admin@example.com
密码: admin123
角色: admin
状态: active
权限: 所有功能
```

### 测试用户 ✅ 已修复
```
用户名: testuser 或 user@example.com
密码: test123
角色: user
状态: active
权限: 普通用户权限
```

### 其他测试用户
```
用户名: testuser1
邮箱: test1@example.com
密码: (查看 init_test_api_keys_v2.py)
```

---

## 🚀 快速登录测试

### 使用curl测试管理员登录
```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "admin", "password": "admin123"}'
```

### 使用curl测试用户登录
```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username": "testuser", "password": "test123"}'
```

---

## ✅ 修复说明

**问题**: 管理员账户密码哈希格式不匹配
**原因**: 数据库使用bcrypt，代码使用SHA-256
**修复**: 统一使用SHA-256更新数据库密码
**结果**: ✅ 所有账户都可以正常登录

---

## 🎯 前端登录

### 访问登录页面
```
http://localhost:3001/login
```

### 输入信息
```
管理员: admin / admin123
用户: testuser / test123
```

### 登录后
- 管理员：可以看到所有用户、管理配额等
- 用户：可以看到个人资料、使用记录等

---

**所有账户现在都可以正常登录！** 🎉
